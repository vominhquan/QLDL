<UserControl x:Class="Applications.Components.Border"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:_="clr-namespace:Applications.Components"
             mc:Ignorable="d">
    <UserControl.ContentTemplate>
        <DataTemplate>
            <DockPanel>
                <Rectangle Fill="{StaticResource GrayLineColor}" DockPanel.Dock="Top" Height="1"/>
                <Rectangle Fill="{StaticResource GrayLineColor}" DockPanel.Dock="Left" Width="1"/>
                <Rectangle Fill="{StaticResource GrayLineColor}" DockPanel.Dock="Right" Width="1"/>
                <Rectangle Fill="{StaticResource GrayLineColor}" DockPanel.Dock="Bottom" Height="1"/>
                <ContentPresenter Content="{TemplateBinding Content}"/>
            </DockPanel>
        </DataTemplate>
    </UserControl.ContentTemplate>
</UserControl>
